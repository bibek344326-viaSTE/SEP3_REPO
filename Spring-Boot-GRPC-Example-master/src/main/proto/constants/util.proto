syntax = "proto3";

package constants;

option java_multiple_files = true;
option java_package = "com.javainuse.constants";

enum Type {
  FANTASY = 0;
  AUTOBIOGRAPHY = 1;
  HISTORY = 2;
}

enum Role {
  InventoryManager = 0;
  WarehouseWorker = 1;
}